name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deployment-info:
    name: Deployment Instructions
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Display deployment options
        run: |
          echo "🚀 Deployment Options for Nebula"
          echo "================================"
          echo ""
          echo "📦 Docker (Recommended):"
          echo "  - Run: docker-compose up --build"
          echo "  - Hosts: Frontend (3000) + Backend (8000)"
          echo "  - Cost: Free (self-hosted)"
          echo ""
          echo "☁️ Cloud Platforms:"
          echo "  1. Railway (railway.app)"
          echo "     - Connect GitHub repo"
          echo "     - Auto-deploys on push"
          echo "     - Cost: $5-20/month"
          echo ""
          echo "  2. Render (render.com)"
          echo "     - Connect GitHub repo"
          echo "     - Free tier available"
          echo "     - Cost: $0-7/month"
          echo ""
          echo "  3. DigitalOcean Droplet"
          echo "     - SSH + Docker deployment"
          echo "     - Full control"
          echo "     - Cost: $6/month"
          echo ""
          echo "  4. Vercel (Frontend) + Railway (Backend)"
          echo "     - Vercel for Next.js"
          echo "     - Railway for FastAPI"
          echo "     - Cost: $0-5/month"
          echo ""
          echo "🔑 Required Environment Variables:"
          echo "  - PINECONE_API_KEY"
          echo "  - TMDB_API_KEY"
          echo ""
          echo "📚 See DOCKER.md for detailed instructions"
